/*
 * ActivityDialog Messages
 *
 * This contains all the text for the ActivityDialog container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'boilerplate.containers.ActivityDialog';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'Select Activities',
  },
});
