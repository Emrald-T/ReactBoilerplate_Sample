// import { selectActivityDialogDomain } from '../selectors';

describe('selectActivityDialogDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
