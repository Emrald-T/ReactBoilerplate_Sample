/*
 * TreeView Messages
 *
 * This contains all the text for the TreeView container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.TreeView';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the TreeView container!',
  },
});
