/*
 * FilterDialog Messages
 *
 * This contains all the text for the FilterDialog container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.FilterDialog';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the FilterDialog container!',
  },
});
