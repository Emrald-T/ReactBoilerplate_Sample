// import { selectFilterDialogDomain } from '../selectors';

describe('selectFilterDialogDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
