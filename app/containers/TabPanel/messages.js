/*
 * TabPanel Messages
 *
 * This contains all the text for the TabPanel container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.TabPanel';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the TabPanel container!',
  },
});
