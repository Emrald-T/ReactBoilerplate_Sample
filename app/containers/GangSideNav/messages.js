/*
 * GangSideNav Messages
 *
 * This contains all the text for the GangSideNav container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'boilerplate.containers.GangSideNav';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'Gangs',
  },
});
